// Copy this file to credentials.h and fill in your values
// DO NOT commit credentials.h to version control!

#ifndef CREDENTIALS_H
#define CREDENTIALS_H

// WiFi Configuration
#define WIFI_SSID     "your_wifi_ssid"
#define WIFI_PASSWORD "your_wifi_password"

// MQTT Broker Configuration
#define MQTT_SERVER   "192.168.1.100"  // Your Home Assistant IP or MQTT broker IP
#define MQTT_PORT     1883
#define MQTT_USER     "mqtt_user"      // Leave empty string "" if no auth
#define MQTT_PASSWORD "mqtt_password"  // Leave empty string "" if no auth

// Device Configuration (used for Home Assistant discovery)
#define DEVICE_NAME   "snap_sensor"    // Unique name for this device

#endif
